# aiogram 3.30 = Bot API 10.2. Апгрейд с 3.7.0 (Bot API 7.4) сделан ради Rich
# Messages из 10.1: заголовки, списки и ТАБЛИЦЫ прямо в чате. До них отчёт в
# чате был нечитаемой простынёй, поэтому вся глубина жила только в WebApp
# (T3.3) — ограничение снято.
aiogram==3.30.0
aiohttp==3.9.5
fastapi==0.115.0
uvicorn==0.30.0
psycopg2-binary==2.9.9
# asyncpg — нативный async-драйвер Postgres (миграция async_db → asyncpg,
# задача #21). aiosqlite — async-бэкенд SQLite для тестов/локалки, чтобы тот же
# async-код-путь покрывался без реального Postgres. См. services/adb_core.py.
asyncpg==0.30.0
aiosqlite==0.20.0
redis==5.0.7
# Excel-экспорт аналитики (PR D). Лёгкая зависимость без C-расширений.
openpyxl==3.1.5
# PDF расходных накладных (services/invoice_pdf.py). Тянет системные
# pango/cairo — в образе нужны libpango-1.0-0, libpangoft2-1.0-0, libcairo2,
# libgdk-pixbuf-2.0-0 и шрифты (fonts-dejavu). Импортируется ЛЕНИВО, внутри
# render_invoice_pdf: без системных библиотек падает только сам рендер,
# а не импорт модуля.
weasyprint==70.0
# Юридические документы (services/legal_docs.py): .docx-шаблон с Jinja2-
# плейсхолдерами → заполненный .docx → PDF через LibreOffice. Системно нужен
# libreoffice-writer: без него LibreOffice не открывает .docx вообще и выходит
# с КОДОМ 0, поэтому в коде проверяется наличие файла, а не returncode.
docxtpl==0.20.2
# Арифметика месяцев в графике платежей: 31 января + 1 месяц = 28 февраля,
# timedelta так не умеет. Сейчас приезжает транзитивно — фиксируем явно,
# потому что код от неё зависит напрямую.
python-dateutil==2.9.0.post0
#
# num2words СОЗНАТЕЛЬНО не используется, хотя напрашивается: узбекского в нём
# нет вовсе, а объявленная им зависимость docopt не собирается на современном
# setuptools и роняет установку целиком. Пропись — services/numerals.py.

