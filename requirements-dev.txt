# Тестовые/dev-only зависимости. CI ставит через этот файл (см.
# .github/workflows/ci.yml); локально: pip install -r requirements.txt -r requirements-dev.txt
#
# Версии запинены (==), чтобы CI не «плыл» от обновлений тулчейна:
# линтер/тайпчекер, поймавшие баг сегодня, должны вести себя так же завтра.
pytest==9.0.3
ruff==0.15.13
# httpx нужен FastAPI TestClient (tests/test_requests_api.py). Без него
# starlette.testclient падает на импорте.
httpx==0.28.1
# aioresponses — мок aiohttp-ТРАНСПОРТА (не наших обёрток), чтобы реально
# исполнялась сборка URL/payload в исходящих запросах (tests/test_notifier_send.py).
# 0.7.9 (последний релиз) не знает обязательный `stream_writer` у ClientResponse
# из aiohttp 3.14 — подпёрт шимом в tests/conftest.py до выхода исправления
# (pnuckowski/aioresponses#288); шим сам отключается на исправленной версии.
aioresponses==0.7.9
# Покрытие — как искатель неисполняемых веток, без жёсткого порога.
pytest-cov==7.1.0
# Точечный статический разбор «денежных»/API-модулей (non-blocking в CI).
mypy==2.1.0
# Чтение сгенерированных PDF в тестах юридических документов.
pypdf==6.18.1
# E2E (tests/e2e): Chromium грузит WebApp с живого uvicorn, fetch настоящие.
# Браузер ставится отдельно: `python -m playwright install chromium`; в
# окружениях с готовым бинарём — PW_CHROMIUM_PATH. Без пакета/браузера e2e
# пропускаются, остальные тесты этого не замечают.
playwright==1.62.0
# Perf (tests/perf): бенчмарки чистых функций. Порогов по времени нет —
# цифры в отчёт; регрессии сложности ловят счётчики запросов.
pytest-benchmark==5.3.0
