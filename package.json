{
  "name": "moysklad-webapp-frontend-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Vitest-харнесс для чистых хелперов WebApp-фронта (webapp/static/helpers.js) + jsdom-смоук загрузки app.js. Python-код тестируется отдельно (pytest).",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
